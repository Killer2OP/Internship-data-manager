export interface Student {
    id: string
    name: string
    course: string
    year: number
    enrollmentNumber: string
  }
  
  export interface Assignment {
    id: string
    title: string
    description: string
    dueDate: string
    createdAt: string
  }
  
  export interface FNR {
    id: string
    title: string
    description: string
    date: string
    createdAt: string
  }